package com.oceanview.dto;

public class UserUpdateRequest {
    public String password; // optional (if provided, change password)
    public String role;     // optional (if provided, change role)
}